#include <iostream>
#include "t3solver.h"
using namespace std;

void t3solver::create_tree(Board b) {
  tree = new TreeNode;
  
}//create_tree


void t3solver::populate(TreeNode n, int height) {
  if(height = 0) {
    n = new TreeNode();
  }//if
  else {
    n = new TreeNode();
    for(int i = 0; i < )
  }//else
}//populate

